namespace Modules.Quotations.Domain;

/// <summary>Un solo valor por ahora — el modelo de datos deja "espacio para estados de
/// seguimiento futuros" sin nombrarlos todavía, así que no se inventan acá.</summary>
public enum SaleStatus
{
    Approved
}
